{[ a comment ]}
{# local nelt = require "path.to.nelt" #}
{# local function test(tp: *nelt.Template, str: string) #}
  {{ "<escaped val>" }}
  text
  {{ str }}
  {{- "<unescaped val>" }}
{# end #}
{# return test #}
